import { makeStyles } from "@mui/styles";

export default makeStyles({
  cartComps: {
    width: "100%",
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
  button: {},
});
